<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PetClinicApp.Desktop.MainPage"
             Title="Dashboard">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20">

            <!-- Header -->
            <Label Text="🐾 Pet Clinic Dashboard"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="{StaticResource Tertiary}"
                   HorizontalOptions="Center" />

            <Label Text="Welcome to Pet Grooming &amp; Veterinary Appointment Scheduler"
                   FontSize="16"
                   TextColor="{StaticResource Gray500}"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center" />

            <BoxView HeightRequest="1" Color="{StaticResource Gray200}" Margin="0,10" />

            <!-- Summary Cards -->
            <Grid ColumnDefinitions="*,*,*" ColumnSpacing="15">

                <!-- Total Pets Card -->
                <Border Grid.Column="0"
                        Stroke="{StaticResource Primary}"
                        StrokeShape="RoundRectangle 12"
                        StrokeThickness="2"
                        Padding="20"
                        BackgroundColor="{AppThemeBinding Light=#F0FAF8, Dark=#1A2E2B}">
                    <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                        <Label Text="🐕" FontSize="32" HorizontalOptions="Center" />
                        <Label x:Name="LblTotalPets"
                               Text="0"
                               FontSize="28"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Primary}"
                               HorizontalOptions="Center" />
                        <Label Text="Registered Pets"
                               FontSize="13"
                               TextColor="{StaticResource Gray500}"
                               HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </Border>

                <!-- Total Clients Card -->
                <Border Grid.Column="1"
                        Stroke="{StaticResource Secondary}"
                        StrokeShape="RoundRectangle 12"
                        StrokeThickness="2"
                        Padding="20"
                        BackgroundColor="{AppThemeBinding Light=#FDF8EC, Dark=#2E2A1A}">
                    <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                        <Label Text="👤" FontSize="32" HorizontalOptions="Center" />
                        <Label x:Name="LblTotalClients"
                               Text="0"
                               FontSize="28"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Warning}"
                               HorizontalOptions="Center" />
                        <Label Text="Total Clients"
                               FontSize="13"
                               TextColor="{StaticResource Gray500}"
                               HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </Border>

                <!-- Earnings Card -->
                <Border Grid.Column="2"
                        Stroke="{StaticResource Info}"
                        StrokeShape="RoundRectangle 12"
                        StrokeThickness="2"
                        Padding="20"
                        BackgroundColor="{AppThemeBinding Light=#EEF4F8, Dark=#1A222E}">
                    <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                        <Label Text="💰" FontSize="32" HorizontalOptions="Center" />
                        <Label x:Name="LblTotalEarnings"
                               Text="$0.00"
                               FontSize="28"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Info}"
                               HorizontalOptions="Center" />
                        <Label Text="Total Earnings"
                               FontSize="13"
                               TextColor="{StaticResource Gray500}"
                               HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </Border>

            </Grid>

            <BoxView HeightRequest="1" Color="{StaticResource Gray200}" Margin="0,10" />

            <!-- Clinic Summary -->
            <Border Stroke="{StaticResource Gray200}"
                    StrokeShape="RoundRectangle 12"
                    StrokeThickness="1"
                    Padding="20">
                <VerticalStackLayout Spacing="10">
                    <Label Text="📊 Clinic Summary"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Tertiary}" />
                    <Label x:Name="LblClinicSummary"
                           Text="Loading summary..."
                           FontSize="14"
                           TextColor="{StaticResource Gray600}" />
                </VerticalStackLayout>
            </Border>

            <!-- Quick Actions -->
            <Label Text="Quick Actions"
                   FontSize="18"
                   FontAttributes="Bold"
                   TextColor="{StaticResource Tertiary}"
                   Margin="0,10,0,0" />

            <Grid ColumnDefinitions="*,*,*" ColumnSpacing="15">
                <Button Grid.Column="0"
                        Text="➕ Add New Client"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White"
                        Clicked="OnGoToClientsClicked" />
                <Button Grid.Column="1"
                        Text="🐾 Add New Pet"
                        BackgroundColor="{StaticResource Secondary}"
                        TextColor="{StaticResource Tertiary}"
                        Clicked="OnGoToPetsClicked" />
                <Button Grid.Column="2"
                        Text="📅 Appointments"
                        BackgroundColor="{StaticResource Info}"
                        TextColor="White"
                        Clicked="OnGoToAppointmentsClicked" />
            </Grid>

            <BoxView HeightRequest="1" Color="{StaticResource Gray200}" Margin="0,10" />

            <!-- Today's Appointments (Requirement #10: Operational Dashboard) -->
            <Label Text="📅 Today's Appointments"
                   FontSize="18"
                   FontAttributes="Bold"
                   TextColor="{StaticResource Tertiary}" />

            <Border Stroke="{StaticResource Gray200}"
                    StrokeShape="RoundRectangle 12"
                    StrokeThickness="1"
                    Padding="15">
                <VerticalStackLayout Spacing="6">
                    <Label x:Name="LblTodayCount"
                           Text="Loading..."
                           FontSize="14"
                           TextColor="{StaticResource Gray600}" />
                    <!-- Bugünkü randevuların listesi -->
                    <CollectionView x:Name="TodayAppointmentsList"
                                    EmptyView="No appointments today.">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="*,Auto,Auto" Padding="5,4" ColumnSpacing="10">
                                    <!-- Hayvan adı -->
                                    <Label Grid.Column="0"
                                           Text="{Binding Pet.Name, StringFormat='🐾 {0}'}"
                                           FontSize="13"
                                           VerticalOptions="Center" />
                                    <!-- Randevu türü -->
                                    <Label Grid.Column="1"
                                           Text="{Binding Type}"
                                           FontSize="12"
                                           TextColor="{StaticResource Gray500}"
                                           VerticalOptions="Center" />
                                    <!-- Randevu saati -->
                                    <Label Grid.Column="2"
                                           Text="{Binding AppointmentDate, StringFormat='{0:HH:mm}'}"
                                           FontSize="12"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource Primary}"
                                           VerticalOptions="Center" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>

